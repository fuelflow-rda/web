# FuelFlow Web – local run
# Copy .env.example to .env. From this folder: docker compose up -d
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
