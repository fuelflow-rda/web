# Local Next.js → local API + Staging Supabase (same staging project as relai-api)
# Copy to .env.local for daily dev: cp .env.staging.local.example .env.local

NEXT_PUBLIC_API_URL=http://localhost:3000
